@model IEnumerable<TajHotel.web.Models.FoodCategory>

@{
    ViewData["Title"] = "Index";
}
<body style="background-color:navajowhite">
<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <table class="table" style="background-color:aqua">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FoodCategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FoodCategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FCId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FCId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FCId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>